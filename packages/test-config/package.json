{
  "name": "@claude-sessions/test-config",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "main": "./vitest.config.ts",
  "scripts": {
    "build": "echo 'no build for test-config'",
    "test": "echo 'no tests for test-config'",
    "typecheck": "tsc --noEmit -p tsconfig.json"
  },
  "devDependencies": {
    "typescript": "^5.7.2",
    "vitest": "^2.1.8"
  }
}
