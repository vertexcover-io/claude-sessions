{
  "name": "claude-sessions-infra",
  "private": true,
  "description": "Pulumi program that provisions the Azure deployment (ACR + Container Apps + managed Postgres). Standalone — not a Turborepo workspace member.",
  "main": "index.ts",
  "devDependencies": {
    "@types/node": "^22.0.0",
    "typescript": "^5.5.0"
  },
  "dependencies": {
    "@pulumi/azure-native": "^2.78.0",
    "@pulumi/pulumi": "^3.130.0"
  }
}
